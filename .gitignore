# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# local env files (never commit secrets). The committed .env holds only a telemetry opt-out.
.env*.local

# misc
.DS_Store
npm-debug.log*
