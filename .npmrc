# Supply-chain hygiene
audit=true
fund=false
# Do not run arbitrary lifecycle scripts from dependencies during install.
# (Next.js/Tailwind/React do not need install scripts; if a future dep does, run `npm rebuild <pkg>` deliberately.)
ignore-scripts=true
